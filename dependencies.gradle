ext {
    libraries = [:]
    slf4jVersion = '2.0.3'
}

libraries.slf4j = "org.slf4j:slf4j-api:${slf4jVersion}"

libraries.reload4j = "org.slf4j:slf4j-reload4j:${slf4jVersion}"

// test libraries
libraries.junit = 'junit:junit:4.13.2'

